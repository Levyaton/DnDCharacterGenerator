package com.dnd.DnDCharacterGenerator.domain.spells.ritual;

public enum RitualCasterSpell4level {
    DIVINATION(114),
;
    private final int id;
    RitualCasterSpell4level(int id) { this.id = id; }
    public int getId() { return id; }
}
